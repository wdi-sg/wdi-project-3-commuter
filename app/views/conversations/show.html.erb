<li class="remove_bullet">
  <div class="panel panel-default" data-conversation-id="<%= @conversation.id %>">
    <div class="panel-heading">
      <%= link_to @conversation.opposed_user(current_user).email, '', class: 'toggle-window' %>
      <%= link_to "x", close_conversation_path(@conversation), class: "btn btn-default btn-xs pull-right", remote: true, method: :post %>
      

    </div>



    <div class="panel-body" style="display: none;">
      <div class="messages-list" id="message-box-<%= @conversation.id %>">
        <ul>
          <%= render 'conversations/conversation_content', messages: @conversation.messages, user: current_user %>
        </ul>
      </div>
      <form class="new_message">
        <input name="conversation_id" type="hidden" value="<%= @conversation.id %>">
        <input name="user_id" type="hidden" value="<%= current_user.id %>">
        <input id="submit_enter" name="body" class="form-control"></input>
        <input type="submit" class="btn btn-success" value="Send">
      </form>
    </div>
  </div>
</li>
